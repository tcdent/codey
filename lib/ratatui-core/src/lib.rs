compile_error!(r#"
================================================================================
  ratatui-core SIMD patch not applied!

  Run this command first:
    ./lib/apply-patches.sh

  Then rebuild:
    cargo build
================================================================================
"#);
